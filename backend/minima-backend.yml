version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:53753

functions:
  minima-backend:
    lang: dockerfile
    handler: ./
    image: minima-backend:latest
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    secrets:
      - openai-api-key 